spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    change-log: db/changelog/db.changelog-test-master.yml